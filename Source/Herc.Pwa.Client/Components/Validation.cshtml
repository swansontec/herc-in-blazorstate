@inherits ValidationModel

@if (!ValidationResult.IsValid)
{
  <div id="@Id" class="alert alert-danger">
    <ul>
      @foreach (var validationResult in ValidationResult.Errors)
      {
        <li>@validationResult.ErrorMessage</li>
      }
    </ul>
  </div>
}
